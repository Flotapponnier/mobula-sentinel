{
  "id": "mobula-sentinel",
  "name": "Mobula Sentinel",
  "version": "1.0.0",
  "description": "Autonomous crypto security guardian — rug detection, smart money tracking, and new token scouting via Mobula API across 88+ blockchains.",
  "entry": "./dist/index.js",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Mobula API key — get one free at https://admin.mobula.io"
      },
      "telegramBotToken": {
        "type": "string",
        "description": "Telegram bot token for alerts (optional)"
      },
      "telegramChatId": {
        "type": "string",
        "description": "Telegram chat ID (optional)"
      }
    },
    "required": ["apiKey"]
  }
}
